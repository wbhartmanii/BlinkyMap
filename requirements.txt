opencv-python>=4.5.0
numpy>=1.21.0
requests>=2.26.0
Pillow>=9.0.0
# 3D viewer — install at least one:
vispy>=0.14.0          # GPU-accelerated, smooth click-drag-rotate (recommended)
matplotlib>=3.4.0      # software-rendered fallback (also supports click-drag)
