# BlinkyMap Python dependencies
# On FPP these are installed via apt (python3-numpy, python3-websockets,
# python3-requests). This file documents the minimum compatible versions
# and enables Dependabot CVE scanning.
numpy>=1.24.0
websockets>=15.0.1
requests>=2.28.0
